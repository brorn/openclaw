# Seu(s) user ID(s) do Telegram, separados por virgula (descubra em @userinfobot)
ALLOWED_TELEGRAM_USERS=123456789

# Token do bot — use Docker secrets (recomendado) ou env var:
#   Secrets: echo "SEU_TOKEN" > secrets/telegram_bot_token
#   Env var: TELEGRAM_BOT_TOKEN=123456:ABC-DEF (fallback, menos seguro)

# Nome da instancia (opcional)
# INSTANCE_NAME=OpenClaw
